# Distinctness - round 2

The five round-2 directions from `03-design-research.md` section 4: **1 Studio Dark**, **2 Shader Light**, **3 Scroll Story**, **4 Bento SaaS**, **5 Liquid Metal**.
Commit basis: `fc78f9a`.
Evidence: `docs/goal/shots/contact-sheet.png`, built from `docs/goal/shots/heroes/vN-1280.png` and `docs/goal/shots/heroes/vN-390.png`, captured viewport-only against the dev server on :3030 after each hero settled.
Every value below is read from `components/variations/vN/tokens.css` and `components/variations/vN/hero.tsx`, not inferred from the screenshots.
Round 1 (Broadsheet, Terminal, Nightshift, Swiss Signal, Console) is superseded and appears nowhere in this file.

## Matrix

| | Type family and weight/width | Palette | Layout/grid | Motion | Hero device |
|---|---|---|---|---|---|
| **1 Studio Dark** | Space Grotesk 700, `--v1-display-track: -0.03em`, `--v1-display: clamp(2.5rem, 1.8427rem + 2.6966vw, 4rem)` = 64px at 1280 and 40px at 390, leading 1; Geist 400 at 17/1.6; Geist Mono tabular for the gauge only | void `#07090c`, panel `#111519`, line `#232a31`, text `#e7eaec`, muted `#8c959d`, lamp `#fff3e0`; achromatic, no accent hue | `--v1-cols: 12`, `--v1-gutter: 1rem`, `--v1-max: 82.5rem`, `--v1-pad: 1.25rem`, `--v1-nav-h: 3.5rem`, `--v1-radius: 0px` with `--v1-radius-control: 4px`; five columns of type left, seven of canvas right | `--v1-tick: 150ms` x 16 = `--v1-build: 2400ms` on `--v1-ease: cubic-bezier(0.2, 0.8, 0.2, 1)`, `--v1-open: 180ms` row disclosure, scroll rotation `--v1-drift: 0.2` rad damped at `--v1-damp: 6` | `mark-scene.tsx` behind `next/dynamic({ ssr: false })`: `app/icon.svg` rebuilt as sixteen torus arcs plus crosshair cylinders and a lamp dot, 520px, with a Geist Mono gauge counting `day 00 / 16` to `16 / 16` |
| **2 Shader Light** | Fraunces 400 at `font-variation-settings: "opsz" var(--v2-display-opsz)` = opsz 96, letter-spacing -0.018em, `--v2-display: clamp(2.25rem, 0.716rem + 6.292vw, 5.75rem)` = 92px at 1280 and 36px at 390, leading 1.05; Geist 400 at 17/1.65; no monospace anywhere | paper `#f3f4f1`, raised `#ffffff`, ink `#141614`, muted `#5e635e`, rule `#d8dad4`; shader poles dubai-day `#e2a34b` to dubai-night `#2b3a6b` and mumbai-day `#3e9b8a` to mumbai-night `#1e3a44`, capped at `--v2-field-max: 0.22` | `--v2-page: 71.25rem`, `--v2-columns: 12`, `--v2-gutter: clamp(1.25rem, 0.55rem + 2.9vw, 2.5rem)`, `--v2-nav-h: 4rem`, `--v2-r: 4px` / `--v2-r-lg: 6px`; centred single column, white plinths with no border | no DOM entrance at all: `--v2-field-in: 900ms` opacity fade only, `--v2-hover-in: 160ms` on `--v2-ease: cubic-bezier(0.2, 0.6, 0.3, 1)`, `--v2-drift: 0%` at rest | `two-city-field.tsx`: a `meshGradient` with two poles at `--v2-pole-dubai` 28% and `--v2-pole-mumbai` 62% of the band, hue driven by live `Asia/Dubai` and `Asia/Kolkata` time, plus `<HeroClocks />` printing both times under the headline |
| **3 Scroll Story** | Archivo variable at `font-variation-settings: "wdth" 70` and `font-weight: 700`, `--v3-size-display: clamp(2.125rem, 4.27vw + 1.084rem, 4.5rem)` = 72px at 1280 and 34px at 390, `--v3-leading-display: 0.94`; Geist 400 at 17/1.6; Geist Mono for the date readout only | a four-stage ramp, not a fixed palette: ground `#0e1216` to `#2c3a47` to `#8a8478` to `#f2f1ec` with ink inverting `#f2f1ec` to `#12161a`; one constant, marker `#c8451c` | `--v3-page: 96rem`, `--v3-gutter: clamp(1.25rem, 3.6vw, 4rem)`, `--v3-measure: 34rem`, `--v3-radius: 0`, `--v3-bar-h: 48px`, `--v3-ring: 200px`; full-width bands, no cards | the ramp is driven by scroll position; `--v3-ease: cubic-bezier(0.2, 0, 0, 1)`, tick draw `--v3-enter-step: 25ms` x `--v3-enter-dur: 265ms`, `--v3-hover-dur: 140ms`, `--v3-disclose-dur: 200ms` | a 48px sticky bar of sixteen ticks with a travelling marker and a mono date readout that starts at `11 Aug`, plus `wire-ring.tsx` at 200px in the right margin, which does not mount below 1100px |
| **4 Bento SaaS** | Geist 600 at `--v4-display-tracking: -0.035em`, `--v4-display: clamp(2rem, 0.2rem + 4.2vw, 3.5rem)` = 56px at 1280 and 32px at 390, `--v4-display-lh: 1.05`; Geist 400 at 16/1.55 body, 15px captions; Geist Mono tabular for every board figure | canvas `#e9eaec`, cell `#ffffff`, ink `#111312`, muted `#616770`, border `#d3d6da`, cobalt `#1b34d8` on at most three surfaces, jade `#12805c` for the live dot only | `--v4-max: 1280px`, `--v4-col-gap: 24px`, `--v4-board-gap: 8px`, `--v4-r-cell: 8px`, `--v4-r-chip: 4px`, `--v4-hairline: 1px` going to `--v4-hairline-hover: 1.5px`, `--v4-nav-h: 64px`, split at `--v4-split: 1180px`; cells are the only container and carry no shadow | one orchestrated board pass: `--v4-online: 460ms` with `--v4-stagger: 60ms`, `--v4-hover: 140ms`, `--v4-count: 700ms` counter, on `--v4-ease: cubic-bezier(0.2, 0.7, 0.3, 1)`; no lift, no translate, no shadow | no canvas and no WebGL at all: a four-cell board preview in `hero.tsx` built from `v4-cell--live`, `v4-cell--build`, `v4-cell--outline` and `v4-cell--figure`, where cell size states how much of the work is actually live |
| **5 Liquid Metal** | Archivo variable at `--v5-display-wdth: 125` and `--v5-display-wght: 800`, `--v5-display-tracking: -0.02em`, `--v5-display: clamp(2.5rem, 9.075vw + 0.538rem, 9.25rem)` = 124.8px measured at 1280 and 148px at its 9.25rem cap, `--v5-display-leading: 0.9`; Geist 400 at 16/1.6 | void `#06080d`, steel `#151b26`, chrome `#dce4ee`, text `#e9edf3`, muted `#7c8798`; two-temperature orb lighting only, cold key `#8fb6ff` and warm rim `#f0c08a`; rules are `color-mix(in srgb, var(--v5-chrome) 10%, transparent)` | `--v5-page: 90rem`, `--v5-measure: 78rem`, `--v5-hero-measure: 120rem` for the headline alone, `--v5-gutter: clamp(1.125rem, 4vw, 6rem)`, `--v5-radius: 2px` with `--v5-radius-pill: 999px`, `--v5-sliver: 3px`, `--v5-nav-h: 3.75rem`; slabs, and two 12px nav items in opposite corners | `--v5-dur-build: 1200ms` env-map build, `--v5-dur-hover: 200ms` tilt, `--v5-dur-fade: 600ms`, on `--v5-ease: cubic-bezier(0.16, 0.84, 0.44, 1)`; roughness rises 0.02 to 0.35 as the hero leaves | `orb-scene.tsx` behind `next/dynamic({ ssr: false })`: a metal sphere at `--v5-orb: min(clamp(300px, 29.21vw + 186px, 560px), var(--v5-orb-cap))` whose `CanvasTexture` environment map is the studio's own words, occluding the headline above 900px and dropping below it under 900px |

## The ten pairs

1-2: Studio Dark is Space Grotesk 700 at -0.03em set light on void `#07090c` around one matte wireframe object, while Shader Light is Fraunces 400 at opsz 96 set ink on paper `#f3f4f1` with no object at all and only a two-pole colour field behind the type.
1-3: Studio Dark holds one fixed darkness that never changes value, while Scroll Story declares its ground as a four-stage ramp from `#0e1216` to `#f2f1ec` that the reader drives by scrolling.
1-4: Studio Dark puts one lit canvas object on an ungridded void at `--v1-radius: 0px`, while Bento SaaS ships no canvas at all and fills a bordered board of 8px-radius cells on canvas `#e9eaec`.
1-5: both are dark, but Studio Dark is achromatic with a single warm lamp `#fff3e0` on a hollow sixteen-arc wireframe under a 64px headline, while Liquid Metal is blue-steel with a cold key `#8fb6ff` and warm rim `#f0c08a` on a solid chrome sphere that occludes a 148px expanded headline.
2-3: Shader Light keeps paper `#f3f4f1` at a constant value and lets only its field move at 22 percent opacity, while Scroll Story inverts ground and ink together across sixteen ticks so the page changes value as you read it.
2-4: Shader Light has no borders and no shadows and separates white plinths from an atmospheric field by their own edge, while Bento SaaS has no atmosphere and draws every boundary as a 1px `#d3d6da` hairline.
2-5: Shader Light is a light page whose display face is a 92px Fraunces serif at opsz 96, while Liquid Metal is a dark page whose display face is Archivo at `wdth` 125 and `wght` 800 running to 148px.
3-4: Scroll Story is one continuous scroll spine whose only sticky element is a 48px sixteen-tick date bar, while Bento SaaS is a static status board that reads whole in a single screenshot with nothing bound to scroll.
3-5: the two share the Archivo family and nothing else, since Scroll Story runs it condensed at `wdth` 70 and `wght` 700 over a travelling ground, while Liquid Metal runs it expanded at `wdth` 125 and `wght` 800 over a fixed void `#06080d`.
4-5: Bento SaaS packs bordered cells of 16px body copy and mono figures into the fold, while Liquid Metal leaves the fold nearly empty and gives the headline the whole 120rem hero measure.
