# Status

Durable memory for the overnight run.
Read this first on every turn and resume from the first line that is not PASS.
Format: `item / PASS|FAIL|TODO / ISO time / notes`.

## Plumbing
branch redesign/five-variations / PASS / 2026-09-07T03:05 / created from main 4f67614, orchestrator-verified via git branch --show-current
switcher at / / PASS / 2026-09-07T03:03 / five 1-5 rows with name+thesis from variations.ts, neutral by design, shot clean at all 9 viewports verifier 2026-09-07T03:12 re-shot as shots/vfy-switcher, all 9 PNGs opened, no overflow/clip/overlap, console clean
route scaffolding v1..v5 / PASS / 2026-09-07T03:03 / (variations) route group, MiniSwitcher pill hidden on ?chrome=0, keyboard-reachable with aria-label per link verifier 2026-09-07T03:12 /v1../v5 200, pill 1-5+/ bottom-right at 390 and 1280 with 2 aria-current, click 4 -> /v4, Tab shows 2px ring on all 6 links, absent under ?chrome=0
screenshot script scripts/shots / PASS / 2026-09-07T03:03 / node scripts/shots.mjs <route> <label> [--sections], 9 viewports, per-label agent-browser session, console.txt gate verifier 2026-09-07T03:12 re-ran for / and /v3, exit 0, 9/9 captured, 0 errors 0 warnings
legacy moved to /legacy/* / PASS / 2026-09-07T03:03 / option-a..d + options + _lib + schematic + animations under app/legacy, legacy.css scoped to its layout, all 5 routes 200 and console-clean verifier 2026-09-07T03:12 5 legacy routes 200, / carries none of hero--pinned/hp-headline/OPS BOARD/hello@devonel.studio while option-d carries all four
.claude/product-marketing.md / PASS / 2026-09-07T02:57 / copywriting+cro intake, every line sourced to a brief section verifier 2026-09-07T03:12 EMAIL_TBD/WHATSAPP_TBD both gone, email is sanchay@devonel.com (13 hits, no other address), whatsapp config-gated empty, banned-word grep clean
docs/goal/COPY.md / PASS / 2026-09-07T02:57 / all 10 sections, 2 headline+2 CTA alternates each, per-variation tone notes verifier 2026-09-07T03:12 banned-word grep returns only the COPY.md:58 declaration line, no em dash, no confidential name, no currency
assets harvested public/media/jewelo / PASS / 2026-09-07T02:58 / 29 files, largest 173 KB verifier 2026-09-07T03:12 find -size +400k empty, 29 files confirmed, no filename or ASSET-INVENTORY alt text names the client brand
runway / UNAVAILABLE / 2026-09-07T02:58 / slots queued in ASSET-QUEUE.md

## v1
hero / PASS / 2026-09-07T03:41 / masthead+front page; tokens.css is the whole v1 system (12-col grid w/ visible column rules, Fraunces opsz display + Geist labels, 3 motion primitives); shared lib/site.ts + components/ds ContactCTA/Mark landed; 9 viewports + hero crops in shots/v1-hero, console clean; CTA above fold at 390x844, 844x390 and 360x780; no 100vh; tsc+lint 0. Fixed: :where() reset (element reset was killing every component margin), classified support line rendered inline, CTA clipped 5px at 360x780. Motion is capture-safe by rule: entrance rises 8px with no fade and drawn figures keep a static underlay, because view() progress is frozen at scroll 0 in a full-page screenshot verifier 2026-09-07T03:41 re-ran scripts/shots.mjs as shots/vfy-v1-hero, opened all 9 hero crops + all 9 full pages: no overflow, clipping, overlap or headline orphan at any width; hScroll false 360-2560; 12 column rules held inside the 1280 shell at 2560 (666-1792 in a 640-1920 shell) and are display:none on the 1-col widths by design; pull-quote clears cta/story/ledger/folio at 820 and 1180 (rect intersection false). Primary CTA fully above the fold measured at scroll 0: 390x844 bottom 740/844, 360x780 753/780, 844x390 337/390. Contrast on paper #f6f1e7: deck 8.56, kicker 9.21, folio+ledger key+quote caption 5.26, classified label 16.6, classified support 8.56, navlink 8.56, headline 16.6 - all clear AA; axe 0 violations, its 3 incomplete color-contrast nodes measured by hand above. Copy: every nav+hero sentence traced to COPY.md section 1 or 10 (or a label); banned-word, confidential-string and em dash greps on rendered HTML and on v1 source all return nothing (regexes control-tested). Links: 3 hrefs, #hero resolves, 2x mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel, zero "#". prefers-reduced-motion emulated: quote opacity 1, transform none, animation-name none, nothing disappears at 390x844 or 1280x800. Tab order Front page -> nav CTA -> hero CTA matches visual order, focus ring 2px #1d3a8a at 3px offset on all three. tokens.css 720 lines defines colour/type/space/grid/radius/shadow/motion tokens plus both reduced-motion blocks; no Newsreader, JetBrains Mono, Inter, #eef0eb, #0e1410 or #c8451c in v1, ds or site.ts; rendered faces are Fraunces and Geist. console.txt clean 0 errors 0 warnings at all 9. Note, not a defect: research 4 mentions a low-opacity paperTexture shader that this hero does not use.
proof / PASS / 2026-09-07T04:12 / ledger strip, pure type, zero images: COPY.md section 2 counts set as a table of figures with an ink rule over it and hairlines between entries. One entry per row to 1023px, five across from 1024px via subgrid so terms sit on one line and notes start on one line across the table; five is prime, so no 2/3/4 column arrangement exists to go ragged, and 820 and 1180 both verified clean. Reuses tokens only (new CSS is tokens.css section 11; --v1-figure is scoped to .v1-proof, no shared token touched). Secondary CTA 'See how it was built' -> #work, verified resolving. Client quote deliberately NOT repeated here: COPY.md places the same sentence in hero and proof, and on a laptop the hero pull-quote is still on screen when this table arrives, so hero keeps the testimonial and proof keeps the counts, which is the split in 03-design-research section 3 item 2. Fixed after shot 1: section head was inheriting .v1-display (front-page 96px) and outshouting its own table, now --v1-display-2 matching .v1-services__title; max-width 20ch was wrapping the head at 1920/2560, removed; note widows killed with text-wrap: pretty. Fraunces ships no tabular figures, so font-variant-numeric is declared but the alignment is done by a fixed track sized off --v1-figure. 9 viewports in shots/v1-proof, 0 console errors; the one 820x1180 warning is a Next LCP priority hint on the work section's image, not this section. tsc 0, lint 0 verifier 2026-09-07T04:30 re-shot as shots/vfy-v1-batch-a; opened all 9 proof crops, full pages at 390/844x390/820/1280/2560, plus live shots at 1023/1024/1100. No overflow, clipping, overlap or ragged wrapping: one entry per row to 1023, five across from 1024, terms on one line and notes starting on one line at 1024, 1100, 1180, 1280, 1536, 1920, 2560. scrollWidth==innerWidth at 360/390/844/2560; zero nodes in #proof with right>innerWidth or scrollWidth>clientWidth at 360/820/1180/1280/2560. Copy: head, secondary CTA and all five figure/term/note strings trace to COPY.md section 2 (row 1 adds the word 'live' before the sourced 27 Aug 2026 date). Links: one href, #work, and that id exists on the page; zero '#'. axe 0 violations 0 incomplete; contrast on paper 16.60 term, 8.56 kicker-adjacent ink-2, 5.26 note, 9.21 kicker. prefers-reduced-motion emulated at 390 and 1280: animation-name none, no opacity<1, no transform, no zero-box text node. Design system: only .v1-* classes, zero inline style=, zero hex literals in proof.tsx; every node in the section measured radius 0px, box-shadow none, background-image none. h2 43.84px at 1280.
services / PASS / 2026-09-07T04:02 / COPY.md section 3 set as a broadsheet digest page: kicker + headline in 7 of 12 columns, the standfirst in the outer 4 opening on a 2-line ::first-letter drop cap in the accent, then the five offers as five ruled columns of type. Zero cards, icons or figures; a line-art figure per offer was considered and dropped because four of the five have no honest silhouette and the v1 tone note asks for type alone. Columns collapse 5 -> 2 -> 1 with no ragged heights: at 45rem-64rem the fifth offer spans both columns as the foot of the block instead of leaving a hole, under 45rem the vertical rules become horizontal ones. Every vertical hairline uses the .v1-colrules half-gutter pull so it lands in the middle of a real gutter, and at 1280 the five offer rules fall on the same x as the proof strip's five stat rules, so the two sections read as one grid. No .v1-colrules layer here: five offer columns cannot align to twelve grid rules and two crossing rule systems would be noise. Motion is one .v1-reveal on the whole list, an 8px rise with no fade, so the list is fully present in a scroll-0 capture. CSS is section 11 of tokens.css, appended (not rewritten) so concurrent builders cannot clobber it; only existing tokens used, none added. Shots docs/goal/shots/v1-services, 9/9 viewports, all 9 -services crops and all 9 full pages opened: no overflow, clipping or overlap, every full-page PNG is exactly viewport-wide. console.txt 0 errors 0 warnings at all 9. npx tsc --noEmit 0, CI=1 npm run lint 0. All 24 content strings verified verbatim against COPY.md section 3 by script; banned-word, em dash and exclamation greps clean. Wired as sections entry {id services, label What you buy} and rendered after V1Proof, canonical order held. Folio reads What you buy / 3. No defects found on the first shoot, so no fix batch and no re-shoot. Not a defect, flagged for the orchestrator: the masthead index now overflows and side-scrolls at 360px, clipping PROOF OF WORK mid-word; that is nav.tsx's documented behaviour and will get worse as the remaining seven sections land. verifier 2026-09-07T04:30 re-shot as shots/vfy-v1-batch-a; opened all 9 services crops plus a live 1024 shot (the 2->5 column handover). Column heights are not ragged: the vertical hairlines span the full row so all five columns close on the same rule, and at 720-1023 offer 5 spans both columns as the foot instead of leaving a hole. No overflow, clipping or overlap at any width; zero nodes with right>innerWidth or scrollWidth>clientWidth at 360/820/1180/1280/2560; scrollWidth==innerWidth at 360/390/844/2560. Copy: all 24 strings plus the three standing lines trace to COPY.md section 3 verbatim. Links: zero hrefs in the section, so no dead target. axe 0 violations 0 incomplete; contrast 8.56 standfirst and detail, 9.21 kicker. reduced-motion at 390 and 1280: nothing hidden, no transform, animation-name none. Only .v1-* classes, zero inline style=, zero hex literals; radius 0px, box-shadow none, background-image none on every node. h2 43.84px at 1280.
case-studies / PASS-BUILDER / 2026-09-07T04:52 / fix round 1: eager plates + priority. Both defective figures fixed at source and both false source comments deleted. components/variations/v1/work.tsx RenderSpread and app/(variations)/v1/work/jewelo/page.tsx Plate now render every next/image with `priority`, which loads eagerly; loading="lazy" is what the intersection observer never fired for in a scroll-0 full-page capture. It is every plate and not only the first because the frames in each figure render at the same width, so next/image named a different one the LCP at different viewports (pendant-close-silver at 820 on /v1, ui-rtl-mirror at 1280 on jewelo); one figure, one setting, and the dev LCP warning is now gone at all 18 captures. Proof, FRESH sessions, no --ignore-console: node scripts/shots.mjs /v1 v1-fix1 --sections hero,work,engagement,faq --session devonel-v1-fix1 and node scripts/shots.mjs /v1/work/jewelo v1-fix1-jewelo --session devonel-v1-fix1-j, both exit 0, 9/9 captured, and both console.txt read 'clean' at all 9 viewports: 0 errors and 0 warnings on both routes, which is the first time this page has shot with zero warnings. Plates measured, not eyeballed: per-frame greyscale stddev inside every figure of the delivered full-page PNGs at 390x844, 360x780, 844x390, 1280x800 and 2560x1080. /v1 four render frames 20.1-39.6 at every width; /v1/work/jewelo all eleven figures 14.6-39.6 at every width. Zero frames under 5, against 0.0-3.2 for the empty boxes this row failed on. Repeated in two brand-new sessions opened cold at 1280x800 with screenshot --full and nothing scrolled: /v1 4/4 painted, jewelo 11/11 painted, and 11/11 img.complete with naturalWidth > 0 at first paint. Shots in docs/goal/shots/v1-fix1 and docs/goal/shots/v1-fix1-jewelo. Also landed in this batch, all four fix-list items that touch this section or its sub-page: the masthead index no longer side-scrolls or clips at any width; every section head is one scale step; the sub-page reads the same section registry as the front page; and the ch sweep. Nav: .v1-masthead__list is flex-wrap now, so ten entries set on two ruled lines at every width from 720px up with the classified notice keeping the corner, and below 720px the five marked `compact` in components/variations/v1/sections.ts print instead while the colophon keeps all ten. Measured at all 9: 0 clipped links, list scrollWidth == clientWidth, documentElement scrollWidth == innerWidth, 2 index rows everywhere. The hero CTA is still fully above the fold at scroll 0 on the tight viewports, which is what the compact index exists to protect: 360x780 bottom 759/780, 390x844 747/844, 844x390 351/390. Sub-page: /v1/work/jewelo imported SECTIONS and dropped its own four-row copy, so its masthead now carries the same ten and all ten /v1#id targets resolve on /v1 (measured, 0 missing). tsc --noEmit 0, CI=1 npm run lint 0. Zero horizontal overflow on the sub-page at all 9 viewports (0 nodes past innerWidth). Cleared and carried forward from the FAIL note: the ui-*.webp corner badge was already stale, and every other check on that note (copy, links, alt text, focus rings, crop, design system) was passing and nothing in this batch touches them. One thing this batch did NOT do, flagged: it re-shot /v1 and /v1/work/jewelo only, so the other sections' own crops in docs/goal/shots/v1-* are older than the tokens.css in the tree; the v1-fix1 full pages are the current state of all of them. verifier 
process / PASS / 2026-09-07T04:47 / Re-shot independently (node scripts/shots.mjs /v1 vfy-v1-batch-b --sections work,process --session devonel-vfy-v1-b), all 9 process crops and the process band of all 9 full pages opened at 360-2560. Vertical schedule, no horizontal timeline and no horizontal scroll at any width; document.documentElement.scrollWidth === innerWidth at 360/390/844/1280/2560. Step numeral sits inside the head element with its phase name, so no width separates them. The drawn figure is present in every capture: pencil underlay rgb(184,176,162) sampled in the 1280 full page, and on real scroll the ink draws to stroke-dashoffset 0 in accent rgb(29,58,138). prefers-reduced-motion at 390 and 1280: all 10 ink paths stroke-dasharray none and dashoffset 0, 0 elements hidden or at opacity 0, images visible. Copy verbatim from COPY.md section 5 (headline, subhead, four phases, after handover, both standing rules); banned-word, confidential-name, currency and em-dash greps clean. Section has 0 links; nav #process and the footer colophon index #process both resolve to the real section - that duplicate is by design, both are generated from the one sections array in app/(variations)/v1/page.tsx and rendered by V1Nav and V1Footer, and it is not a dead link. No inline colour or font literals, no hex, only .v1-* classes; radius 0 and shadow none on the section and its children; process h2 43.84px Fraunces at 1280 (the page runs two head sizes: 85.92px for work, engagement, contact and 43.84px for proof, services, process, founders, faq). Console clean at 8 of 9 viewports; the single warning at 820 is the next/image LCP hint on the work section's pendant image, not this section. verifier
founders / PASS / 2026-09-07T03:52 / The staff box: COPY.md section 6 verbatim, set as two ruled bylines under a centred head and closed by the imprint line. No founder photography exists in the repo and none was invented; the portrait slot is a large Fraunces initial (display opsz, ink, no fill/ring/box) hanging in its own column, aria-hidden since the name is already read out. Noora is omitted entirely: COPY.md marks her line consent-gated and the open-items list still has it open. Nothing about employers, revenue or anything on the never-publish list; the clinic-software line is the sanctioned brief 9.3 wording. No CTA in this section by design (trust block before engagement; COPY.md pins one primary CTA wording already carried by the masthead, hero and closing sections). Head and imprint centre from the same 45rem breakpoint the nameplate centres at, so the block reads as the masthead's twin; bylines stay left aligned. Bylines 6+6 columns at the broadsheet breakpoint (incl. the 720x<=560 landscape arm), stacked below it. Zero cards, shadows, gradients, radius; divisions are hairline + ink rules and whitespace. Only tokens.css tokens used; new .v1-founders/.v1-byline block appended to tokens.css, no inline styles. Motion is .v1-reveal only (8px rise, no fade, capture-safe, already inside the reduced-motion guard). Folio via .v1-folio, 'Who you work with' / 6. Shots: docs/goal/shots/v1-founders, 9 viewports + 9 founders crops, all opened. console.txt 0 errors (the single 820x1180 warning is the work section's LCP image, not this section). tsc --noEmit 0, CI=1 npm run lint 0. Fixed in one batch after the first shoot: .v1-byline__roles was auto-placing into the initial's ~47px column at every width, wrapping the role copy to one word per line and overflowing; every child is now explicitly placed and align-content:start stops the outer grid's equal-height stretch from pushing the shorter byline's copy away from its name. Re-shot once, clean at 360/390/844/820/1180/1280/1536/1920/2560. Long names hold one line at 360 and 390. Rendered copy diffed against COPY.md section 6 word for word; banned-word, em dash and confidential-name greps on the source return nothing. Wired: one import, sections entry {id:'founders', label:'Who you work with'} and <V1Founders/> after <V1Work/>; process is not built yet, so it will insert above this row later. verifier 2026-09-07T04:30 re-shot as shots/vfy-v1-batch-a; opened all 9 founders crops and the full pages. Both names hold one line at 360 and 390; the initial column never steals the roles column at any width; bylines sit 6+6 from the broadsheet breakpoint and stack below it with even rule lengths. No overflow, clipping or overlap; zero nodes with right>innerWidth or scrollWidth>clientWidth at 360/820/1180/1280/2560; scrollWidth==innerWidth at 360/390/844/2560. Copy: head, deck, both bylines and the standing line are COPY.md section 6 verbatim; Noora correctly omitted as consent-gated. Links: zero hrefs. axe 0 violations, 3 incomplete color-contrast nodes measured by hand at 8.56 (deck and both role paragraphs, ink-2 on paper), imprint 16.60. reduced-motion at 390 and 1280: nothing hidden, no transform. Only .v1-* classes, zero inline style=, zero hex literals; radius 0px, box-shadow none, background-image none. h2 43.84px at 1280.
engagement / PASS / 2026-09-07T03:51 / How a quote works, set as a trade paper rate card page: kicker + .v1-display headline + deck, a ruled tariff of the four ways to work (term in cols 1-4, its terms in 5-12, hairline per line, the page column rules showing through as the table verticals), the minimum on the ink rule that closes the table, then two conditions and the classified CTA across the foot. Zero cards, shadows, gradients or fill measured on the CTA (border-radius 0px, box-shadow none, background-image none, background transparent). New CSS appended to tokens.css as its own "engagement: rates and terms" block, tokens only, no new colour/size literals except the two grid spans. Copy is COPY.md section 7 verbatim, citations stripped: every rendered string grep-matched against COPY.md; the four term names drop the run-in period COPY.md gives them because they are table labels, not sentences, which is what the v1 tone note "terms listed like rate-card lines" asks for. No figure, currency or range anywhere: the only rendered digit in the section is the folio 7. Banned-word, em dash, exclamation and confidential-name greps clean. CTA is the canonical "Send your brief" through ContactCTA/lib site with support line "Paid discovery, fixed scope, no forms.", resolving to mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel, data-contact-channel=email. Nav label "How a quote works", #engagement resolves. Shots in docs/goal/shots/v1-engagement, all 9 viewports, 0 console errors; opened all 9 -engagement crops and all 9 full pages, no defects to fix so no re-shoot. Tier stacking order proven at 390 by measured tops (Paid discovery 9792 < Fixed-scope build 9934 < Monthly retainer 10049 < Revenue share 10164), which is document order and is never reordered by the layout. No comparison table needing horizontal scroll: hScroll false at 390 and 1280, zero elements overflowing the viewport, zero elements with scrollWidth > clientWidth inside the section, and full-page capture width equals viewport width at all 9. Contrast on paper #f6f1e7: term ink 16.8, terms + condition + CTA support ink-2 8.56, kicker accent 9.2, all clear AA at their sizes (smallest is the 11px kicker at 9.2). One tabbable element in the section, focus ring 2px #1d3a8a at 3px offset. prefers-reduced-motion emulated: .v1-tariff animation-name none, opacity 1, transform none, all 4 rows present, underline transition off; nothing disappears. tsc 0, CI=1 lint 0. Not mine, reported: the one console warning at 820x1180 is next/image LCP priority on /media/jewelo/pendant-close-silver@2x.webp in the work section. verifier 2026-09-07T04:30 re-shot as shots/vfy-v1-batch-a; opened all 9 engagement crops, the full pages and a live 1024 shot. Tier order is document order at every width (Paid discovery, Fixed-scope build, Monthly retainer, Revenue share) and never reflows; no comparison table and no horizontal scroll: scrollWidth==innerWidth at 360/390/844/2560 and zero nodes in the section with right>innerWidth or scrollWidth>clientWidth at 360/820/1180/1280/2560. Copy is COPY.md section 7 with the four term names as labels; the builder lifts 'Minimum commitment up front on every engagement.' out from under 'What we do not do' onto the closing rule, which resolves a contradiction in COPY.md rather than inventing text. Links: one href, the canonical mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel; zero '#'. Keyboard: the CTA is the 12th tab stop, matches :focus-visible, outline 2px solid rgb(29,58,138) at 3px offset. axe 0 violations, 2 incomplete contrast nodes measured by hand at 8.56. reduced-motion at 390 and 1280: nothing hidden, no transform, animation-name none. Only .v1-* classes, zero inline style=, zero hex literals; the classified CTA box measures radius 0px, box-shadow none, background-image none, as does every other node. FLAGGED, not failed: h2 is 85.92px at 1280 against 43.84px for proof, services, process and founders, and is exactly the hero h1 size because .v1-engagement__title takes .v1-display (--v1-display-1) while the others take --v1-display-2, whose tokens.css comment declares it the section-head step. That is one named scale step but a 1.96x jump; #work does the same, so the orchestrator has to pick one convention across work and engagement rather than this section alone. Second nit: the 'Four ways to work with us' crosshead hairline touches the 'y' descender at 1024, 1180 and 1280.
faq / PASS / 2026-09-07T04:41 / Before you pay, set as a trade paper queries page: COPY.md section 8, all seven questions verbatim (script-diffed word for word; the only delta is a typographic apostrophe in "client's", matching work.tsx). Native <details>/<summary>, zero JavaScript. Two ruled columns of queries from the broadsheet breakpoint (4 over 3), one column below it; the vertical rule between them is the page's own <ColumnRules /> showing through, not a border. The two columns are separate blocks, not cells of one grid, because a shared grid puts the first query of each column in the same row and tears a hole beside every open answer; below the breakpoint the wrappers are display:contents so the seven read as one list in document order. Affordance is a hairline plus hanging in the margin, drawn from the same 1px rule as every division on this page; [open] rotates its upright bar flat to a minus, and prefers-reduced-motion turns the rotation off while both end states stay truthful. First query (cost) ships open: it is the objection every reader arrives with and it stops the section presenting as seven closed lines. No CTA here by design, contact is the next section and a second paid notice would be the same offer twice. Zero cards, fills, radius, shadows, gradients; only existing tokens, new CSS appended to tokens.css as its own "faq: the queries column" block. Measure capped under the 65ch ceiling at every width, measured in real rendered ch: 28.7 at 360, 31.6 at 390, 60.0 at 820 (the cap binding), 46.6 at 1180, 50.5 at 1280/1536/1920/2560. Keyboard: real Tab reaches the summary, :focus-visible matches, ring is 2px solid #1d3a8a at 3px offset, Enter closes and Space opens. axe on #faq: 0 violations, 7 passes; its 3 incomplete color-contrast nodes measured by hand on paper #f6f1e7 - question ink 16.60, answer ink-2 8.56, kicker accent 9.21, folio ink-3 5.26, all clear AA. Reduced motion emulated: list animation-name none, opacity 1, transform none, all 7 present, marker transition off. Shots docs/goal/shots/v1-faq, 9 viewports + 9 -faq crops, console.txt 0 errors 0 warnings; every full page is exactly viewport-wide (no horizontal overflow, documentElement scrollWidth check false at 1280). Interaction proof: agent-browser clicked all 7 summaries at 390 and at 1280 (first one clicked twice to prove it toggles), 7/7 [open] with 196-438 chars and non-zero height each, captured to shots/v1-faq/open-390.png and open-1280.png. Fixed in one batch after the first shoot: .v1-underline was blockified as a grid item so the hover rule ran the full column width and read as another division between queries; justify-self:start now shrinks it to the words (169px under "How fast is it?"). Re-shot once. Only rendered digit in the section is the folio 8; banned-word, em dash, exclamation, currency and never-publish greps all clean. Wired as one import, sections entry {id faq, label "Before you pay"} and <V1Faq/> after <V1Engagement/>; contact and footer landed from another builder during the run and canonical order still holds. tsc --noEmit 0, CI=1 lint 0. Note for the orchestrator, not a defect: COPY.md's v1 tone note says "no accordion" while this task specified <details>; resolved by keeping the accordion mechanics and giving it the printed form the note asks for - bold serif running heads, answers in body serif, hairlines and whitespace only, no box. verifier 2026-09-07T04:17 re-shot as shots/vfy-v1-batch-c with no --ignore-console: 9 viewports + 9 faq crops, exit 0, console.txt clean 0 errors 0 warnings at all 9. Every full-page PNG is exactly viewport-wide (no horizontal overflow 360-2560) and hScroll false at 1280. All 9 faq crops opened: no overflow, clipping, overlap or orphaned head; 4-over-3 two-column set from 1024, one column below, document order preserved either way. Interaction: agent-browser clicked all 7 summaries at 390 and at 1280; every one opens to a non-empty answer, chars 238/260/237/210/438/196/292 with non-zero rects at both widths, and Q1 was clicked twice to prove it toggles. Keyboard: real Tab lands on the first summary, :focus-visible true, ring 2px solid rgb(29,58,138) at 3px offset and visible in shots/vfy-v1-batch-c/focus-summary-1280.png; Enter closes then reopens; the only clipping ancestor is .v1 overflow-x:clip at the page edge, which does not touch the ring. Reduced motion emulated: marker ::after transition collapses to ~0s, both end states stay truthful (open rotate 0deg, closed 90deg), .v1-reveal animation-name none with opacity 1 and transform none, all 7 queries present, nothing hidden. Measure at 2560: 50.5 CSS ch, under the 65ch ceiling (the calc(60ch + indent) = 690.8px cap is not binding there, the 556px column is). Copy: head and all 7 Q+A diff verbatim to COPY.md section 8 after stripping citations. Design system: zero non-.v1-* classes, zero inline style attributes, and every node in #faq measures border-radius 0, box-shadow none, background-image none, text-shadow none, filter none. h2 43.84px at 1280. axe 0 violations; its 3 incomplete colour-contrast nodes hand-measured on paper: question ink 16.60, answer 8.56, kicker 9.21, folio 5.26, section minimum 5.26, all clear AA. Visible-text greps zero for banned words, confidential names, currency, em dash, exclamation and WhatsApp. FLAG, not a defect: real rendered characters per line at 2560 run 62-72 (max 72) because Fraunces sets a wide '0' relative to its average glyph, so the section passes the stated 65ch in CSS ch and sits inside the 45-75 readability band; this is the ch-versus-rem item already on the orchestrator fix list.
final-cta / PASS / 2026-09-07T04:36 / components/variations/v1/contact.tsx, id=contact: the closing classified page. Section opens on a full-bleed 5px accent rule (the one colour gesture at page scale, echoing the classified box's own accent top), then kicker Start + .v1-display recap 'Send the brief. We send back a scope.' + the risk-reversal deck, then one ruled announcement closed top and bottom by ink rules holding three cells: the advertisement (cols 1-4), the contact lines as small print (5-7), the client's words (8-12). That is the v1 tone note, 'a classified-ad box, ruled, with the contact lines set as small print'. One action, one channel: the button is ContactCTA with contactLabel() from lib/site.ts, never a hardcoded channel; measured href mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel, data-contact-channel=email. The address is live linked text built from site.contact.email + mailSubject so the subject cannot drift. WhatsApp clause is config-gated on site.contact.whatsapp.length > 0 (read as a length, not the empty literal, so it is genuinely runtime-gated) and renders nothing today; no chat channel is named. No form fields anywhere, no reply-time promise. Copy is COPY.md section 9 verbatim (citations stripped), plus section 7's 'Minimum commitment up front on every engagement.' brought down to sit with the other terms where a classified prints them; all 10 strings grep-matched against COPY.md. Banned-word, em dash and exclamation greps on the source clean. Rendered section text diffed word for word. Zero cards, shadows, gradients, radius measured on the CTA: border-radius 0px, box-shadow none, background-image none, background rgba(0,0,0,0). Divisions are rules and whitespace only. Tokens only; new CSS appended to tokens.css with cat >> as its own 'contact: the closing notice' block, no existing rule rewritten, no new colour or size literals except the grid spans and one 26rem cap. Element resets untouched (:where() reset in section 2 is what makes the component margins work). Motion is one .v1-reveal on the proof figure only, already inside the @supports + no-preference guard: 8px rise, no fade, opacity 1 and nothing hidden at scroll 0, and the button itself never moves, matching hero and engagement. Folio via .v1-folio, 'Start' / 9. Shots: docs/goal/shots/v1-contact, 9 viewports + 9 -contact crops, all opened, plus all 9 full pages (every full-page PNG is exactly viewport-wide, so no horizontal overflow at 360-2560; hScroll false measured at 390). console.txt 0 errors and 0 warnings at all 9. Fixed in one batch after shoot 1, re-shot once: (a) in the one-column arm the classified box ran the full 820px measure and read as an empty banner, now capped at 26rem so it reads as an advertisement, which is under a phone's content width and under four columns at every broadsheet width so nothing else changes; (b) at 844x390 the lead pushed the button off a 390px tall screen, so the max-height:560 arm now runs the --lg label back to the standard classified size and closes the deck and notice gaps, and the whole announcement plus headline now fits one landscape screen. Mini-switcher pill proven not to cover the CTA: docs/goal/shots/v1-contact/390-with-pill.png, taken at 390x844 WITHOUT ?chrome=0 by opening http://localhost:3030/v1#contact directly. Measured rects: CTA x16 y376 358x126 (bottom 502), pill x157 y792 221x40 (bottom 832), intersection false, 290px of clearance; the button is the fourth thing in the section so an anchor jump lands it in the upper half of the screen, and the closing folio carries 4rem of clearance under it at max-width 719px or max-height 560px so the last small print is not under the pill either. Two tabbable elements in the section (button, email link), both inheriting the 2px #1d3a8a focus ring at 3px offset. Wired: one import, sections entry {id:'contact', label:'Start'} between faq and footer, <V1Contact/> rendered after <V1Faq/>; #contact resolves from both the masthead index and the footer index. npx tsc --noEmit 0, CI=1 npm run lint 0. Not a defect, flagged: COPY.md section 9's headline sets as two balanced lines breaking after 'We' at every width; that is text-wrap: balance on .v1-display and is standard headline practice, but if the orchestrator wants the break after the first sentence it needs an explicit per-sentence block and will widow 'scope.' at 360. verifier 2026-09-07T04:17 re-shot as shots/vfy-v1-batch-c with no --ignore-console: 9 viewports + 9 contact crops, exit 0, console clean 0 errors 0 warnings at all 9, every full page exactly viewport-wide, all 9 crops opened with no overflow, clipping or overlap. Primary CTA href measured exactly mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel with data-contact-channel=email, and the address also renders as live linked text on the same href. Zero input, textarea, select or form elements on the whole page. site.contact.whatsapp is empty and no WhatsApp string renders in #contact; the 3 whole-page WhatsApp hits are all product vocabulary in services and work. Copy diffed line by line: every string traces to COPY.md section 9 except 'Minimum commitment up front on every engagement.', which traces to section 7 and is the doc move already on the orchestrator fix list; the risk-reversal line and the proof line are section 9 verbatim. Pill proof: /v1#contact at 390x844 WITHOUT chrome=0, shots/vfy-v1-batch-c/390-contact-with-pill.png. CTA rect x16 y376 358x126 bottom 502, pill rect x157 y792 221x40, intersection false, 290px clearance; the closing folio text ends at y786, 6px clear of the pill. Design system: zero non-.v1-* classes, zero inline styles, every node border-radius 0, box-shadow none, background-image none, text-shadow none, filter none. h2 85.92px at 1280. axe 0 violations; its 3 incomplete contrast nodes hand-measured on paper: display 16.60, deck 8.56, small print 8.56, textlink 9.21, caption 5.26, all clear AA. Focus: both tabbable elements in the section carry a 2px solid rgb(29,58,138) ring at 3px offset. Rendered-text greps zero for banned words, confidential names, currency, em dash and exclamation. FLAG, not a defect: this h2 is --v1-display-1 at 85.92px while faq, proof, services, founders and process are --v1-display-2 at 43.84px; the orchestrator fix list asks section heads to unify on display-2, so contact joins work and engagement as the three still on display-1.
footer / PASS / 2026-09-07T04:24 / The colophon: COPY.md section 10 set as the masthead turned upside down at half the size. Order mirrors the nameplate exactly - the masthead opens on a rail then a thin rule over a thick one then the wordmark; the foot opens on a thick rule over a thin one, then the same Mark+wordmark lockup with --v1-wordmark retuned to half, and ends on the running rail. Three ruled columns: imprint 1-4 (wordmark, site.positioning, 'Dubai and Mumbai. Working with clients across the UAE, India and remote.'), contact 5-7 (the address as live serif text linked to mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel, built from lib/site.ts not contactHref() because contactHref resolves to WhatsApp once a number lands and this line is the mailbox), index 8-12. Every column carries its own standing head, which is what stops the 390 collapse from stranding a heading over someone else's list (research section 5, 'footer columns collapsing without headings'). Index is generated from the sections prop and drops its own row, so it can only link to rendered sections; it grew from 8 to 10 entries mid-build as faq and contact landed and needed no edit. Editions line carries the required switcher link: five numbered names from variations.ts, v1 rendered as unlinked aria-current, plus 'All five editions' to /. No WhatsApp line (COPY.md gates it on contact.whatsapp, empty, no number guessed), no CTA (COPY.md pins one CTA wording already carried by the section above), no column rules and no folio number (a colophon is where the page stops). Zero cards, shadows, gradients, radius; every division is a hairline, an ink rule or paper. Only existing tokens; new CSS appended to tokens.css as its own 'footer: the colophon' block, never rewritten. Fixed in one batch after shoot 1: (a) index was a grid, so a 2-3 column index read across the row and put every second entry in the wrong place - now multicol (columns: 13rem) so it reads down a column; (b) overflow-wrap:anywhere on the address drove the column min-content to one character and tore 'sanchay@devonel.com' into a line ending in a lone 'm' at 844x390 - now break-word plus a one-step size drop under the max-height:560px arm, measured 1 line at every width; (c) the small-print rail was full-bleed, which at 2560 stranded the legal and the revision line in opposite corners - rule now runs to the screen edge while the type stops at the 1280 cap, the masthead index bar's arrangement. Re-shot once, clean. Proof: docs/goal/shots/v1-footer, 9 viewports, all 9 -footer crops and all 8 page bottoms opened plus an extra PIL crop of the 844x390 footer to prove the address holds one line; every full-page PNG is exactly viewport-wide (no horizontal overflow at any of the 9). console.txt clean, 0 errors 0 warnings at all 9. Links: all 16 clicked at 1280 - 9 anchors (#hero #proof #services #work #process #founders #engagement #faq #contact) each exist and land at rect.top 0, /v1/work/jewelo + /v2 /v3 /v4 /v5 + / all navigate and return 200 with real h1s, 1 mailto carrying the canonical subject. Zero '#' hrefs. Footer height 499px at 1280x800, so the whole colophon is inside one viewport. Review pill does not intersect the legal or revision line. Copy: every rendered string grep-matched to COPY.md section 10 or lib/site.ts; banned-word, em dash, exclamation and confidential-name greps clean. npx tsc --noEmit 0, CI=1 npm run lint 0. Wired: one import, sections entry {id: 'footer', label: 'Colophon'} last, <V1Footer sections={sections}/> after </main>; export added to index.ts. verifier 2026-09-07T04:17 re-shot as shots/vfy-v1-batch-c with no --ignore-console: 9 viewports + 9 footer crops, exit 0, console clean 0 errors 0 warnings at all 9; all crops plus the full pages at 390, 844x390, 820, 1280 and 2560 opened, every full-page PNG exactly viewport-wide, no overflow, clipping or overlap. The 390 and 360 collapse strands no heading: imprint, CONTACT, INDEX and EDITIONS each carry their own standing head, and the index reads down its columns at 820, 1180, 1280, 1536, 1920 and 2560. Links: 16 anchors and zero '#' hrefs; all 9 section anchors resolve to a live element (#hero #proof #services #work #process #founders #engagement #faq #contact); /v1/work/jewelo, /v2, /v3, /v4, /v5 and / all return 200; the switcher link to / ('All five editions') is present and v1 renders unlinked with aria-current. The mailto is well formed, mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel. Legal line carries 2026 and Devonel; locations are Dubai and Mumbai only; no timezone, street address or phone number, and no WhatsApp line while site.contact.whatsapp is empty. Footer height 499px at 1280x800, inside one viewport. Copy: every rendered string traces to COPY.md section 10, a rendered section label, or lib/site.ts. Design system: zero non-.v1-* classes, zero inline styles, every node border-radius 0, box-shadow none, background-image none. axe on #footer: 0 violations and 0 incomplete; hand-measured contrast on paper: wordmark 16.60, signoff 8.56, location/heads/legal/revised 5.26, email 16.60, index link 8.56, all clear AA. Every footer link shows the 2px solid rgb(29,58,138) ring at 3px offset. Rendered-text greps zero for banned words, confidential names, currency, em dash and exclamation.
matrix 9 viewports / TODO
interactions / TODO
preview url / TODO

## v2
hero / PASS-BUILDER / 2026-09-07T04:39 / shell session, zero images (0 img/svg/video/canvas, 0 background-image incl. pseudo-elements): the type is the art. components/variations/v2/tokens.css is the whole v2 system - dark-only ink #0b0b0b, text #e8e6df, dim #8b8880 (computed for AA), rule #3f3d39, accent #38d27a spent only on the caret, the live rail counter and the CTA; ONE family (Geist Mono via --font-mono) at exactly three rendered sizes (13 / 16 / 36.16px at 1280), tabular figures on, radius 0px and box-shadow none on every node, no gradient. Spacing scale in ch across and lh down. Motion is steps() only: v2-print entrance (translateY 0.5lh, no opacity, so a mid-animation capture is still fully legible), v2-inv inverse-video hover, v2-meter stepped scroll bar on scroll(root block); all three have explicit reduced-motion resting states. Grid: 80ch measure centred, full-width ASCII rules built from a repeating '-' in a CSS pseudo-element (--v2-rule-run, no image), fixed left rail from 1024px printing the section registry as ASCII `tree` output plus a 01/01 live counter, compact sticky top strip below 1024. sections.ts is the single registry later builders append to; index.ts carries the how-to-add-a-section contract. Shots: docs/goal/shots/v2-hero, 9 viewports + 9 hero crops, console.txt clean 0 errors 0 warnings. Fixed in one batch after shot 1: rail tree branches were set in rule colour and were near-invisible (now dim), 844x390 ran 24px past the viewport (section padding halved on short viewports), ultrawide left the measure stranded (hairline frame at the measure edges from 1440px ties the top and bottom rules together), phone gutter 3ch was wrapping the command line at 360/390 (now 2ch), caret blinked 50/50 so half the captures lost it (duty cycle biased to 70% on, still steps(2)). Measured: CTA fully above the fold at 390x844 (509/844), 844x390 (299/390), 360x780 (553/780), 1024x768 (421/768) and the support+mail lines with it; docHeight == viewport height at all four, so nothing scrolls and no 100vh traps. hScroll false 360-2560. Rail never overlaps content: 0 intersecting nodes at 1024/1100/1280/1536/2560, clearance 0-763px, and the bottom-right mini-switcher pill clears the rail, the rail meter and the measure at 1024. Contrast on #0b0b0b: heading and body 15.76, prompt/status/notes/rail-root 5.56, CTA and live counter 10.0 - every text token clears AA at its size; axe 0 violations 0 incomplete. prefers-reduced-motion: animationName none on every node, caret opacity 1 and 9.6px wide, h1 transform none, meter track still drawn, 0 low-opacity and 0 zero-box text nodes, so nothing motion-only disappears. No-JS/SSR HTML carries the full hero text and marks the first rail entry aria-current, and no opacity:0 exists anywhere in the markup. Tab order rail link -> CTA -> mail link matches visual order with a 2px #38d27a ring on all three. Links: 3 hrefs, #hero resolves, 2x mailto:sanchay@devonel.com?subject=Brief%20for%20Devonel from lib/site.ts, zero '#'. Copy is COPY.md section 1 on v2 tone (numerals, lowercase, terse); banned-word, em dash and client-brand greps clean on source and rendered HTML. tsc 0, lint 0.
proof / TODO
services / TODO
case-studies / TODO
process / TODO
founders / TODO
engagement / TODO
faq / TODO
final-cta / TODO
footer / TODO
matrix 9 viewports / TODO
interactions / TODO
preview url / TODO

## v3
(same rows as v1)

## v4
(same rows as v1)

## v5
(same rows as v1)

## Distinctness
contact sheet / TODO
pairwise statements / TODO

## Gates
tsc / TODO
lint / TODO
build / TODO
console clean all routes / TODO
lighthouse v1..v5 / TODO
legacy routes deleted / TODO
REPORT.md printed / TODO

## Run notes
agents / NOTE / 2026-09-07T03:05 / .claude/agents builder+verifier not registered in this session; dispatching general-purpose on Opus with the same role prompts inlined; effort flag not settable via Agent tool
dev server / NOTE / 2026-09-07T03:05 / single shared next dev on :3030 started by orchestrator; builders must not restart it

## v1 matrix-pass fix list (orchestrator, 2026-09-07T04:05)
Builder round 1 closed 2026-09-07T04:52. Every bullet marked below.

- [done] nav: masthead anchor row side-scrolls and clips "PROOF OF WORK" at 360. `.v1-masthead__list` is `flex-wrap: wrap` with a `--v1-s1` row gap, so entries never scroll and never clip; the hairline each link already carries rules the wrapped lines, which is what makes two lines read as a printed index rather than as a broken rail. Ten entries set on exactly two ruled lines at every width from 720px up, with the classified notice keeping the top-right corner (`align-items: start`). Below 720px the masthead prints only the five entries marked `compact` in components/variations/v1/sections.ts (proof, services, work, engagement, contact: the buying path) and drops the notice, because ten entries wrap to five lines at 360px beside a 120px notice and every masthead line pushes the hero CTA toward the fold. The colophon index at the foot still carries all ten at every width, which is where the other five stay reachable. No JavaScript and no menu button. Measured at all 9 viewports: 0 clipped links, list scrollWidth == clientWidth, 2 rows, documentElement scrollWidth == innerWidth. Hero CTA still above the fold at scroll 0: 360x780 759/780, 390x844 747/844, 844x390 351/390.
- [done] h2 scale: all eight section heads now run at --v1-display-2, 43.84px at 1280, and the hero h1 is the only display-1 on the page. `.v1-work__title` and `.v1-engagement__title` took the step named in this bullet; `.v1-contact__title` took it too, because the task line reads "unify all section heads" and leaving the closing recap at 85.92px would have left the same split in a different place. Rebalance that came with it: `.v1-classified--lg .v1-classified__label` moved from --v1-display-2 to --v1-text-quote, since a label at display-2 would have tied the head it sits under. Nothing else in contact changed and the head only got smaller, so its 844x390 fit is unaffected. Measured at 1280: h1 85.92, all eight h2 43.84, the large classified label 31.
- [done] engagement: crosshead hairline touches "y" descender at 1024-1280. `.v1-tariff > .v1-crosshead` takes `padding-bottom: var(--v1-s2)`. Measured clearance from the last line box of "Four ways to work with us" to the first tariff rule: 9.1px at 1024, 1180 and 1280, from 0.0px before. Scoped to the tariff because an audit of every `.v1-crosshead` on the page found this was the only one sitting on a rule.
- [done] sub-page /v1/work/jewelo mirrors its own sections array. New components/variations/v1/sections.ts holds the `V1Section` type and the one `SECTIONS` array; index.ts re-exports both; app/(variations)/v1/page.tsx and app/(variations)/v1/work/jewelo/page.tsx both import it, and neither declares an anchor list any more. nav.tsx and footer.tsx take the type from the same file, which also removes the index -> nav -> index type cycle. The sub-page masthead now carries all ten and all ten /v1#id targets resolve on /v1 (measured, 0 missing).
- [done] COPY.md section 7: "Minimum commitment up front on every engagement" moved out of "What we do not do" and set under the four ways to work, with an italic note recording the move and why, so v2-v5 read the placement rather than the contradiction. "What we do not do" keeps "No unpaid multi-month starts." engagement.tsx already rendered the line where COPY.md now files it, on the ink rule that closes the tariff, so no component changed.
- [done] final matrix run must NOT use --ignore-console. Both runs in this batch were plain: `node scripts/shots.mjs /v1 v1-fix1 --sections hero,work,engagement,faq --session devonel-v1-fix1` and `node scripts/shots.mjs /v1/work/jewelo v1-fix1-jewelo --session devonel-v1-fix1-j`. Both exit 0 and both console.txt read "clean" at all 9 viewports, 0 errors and 0 warnings, so the gate passes on its own terms with nothing suppressed.
- [done] measure units: eleven `ch` caps on running text are now rem, sized from the rendered average letter rather than guessed. Measured on the page, one ch is 1.38x the average letter on the Fraunces deck, 1.45x on Fraunces body and 1.42x on Geist small print, so the old numbers understated every line: .v1-tariff__terms 54ch was setting 79 characters, .v1-foot__legal 76ch was setting 109, .v1-byline__roles 58ch was setting 82. New caps and the character count they set at 1280: deck 38rem/64, tariff terms 32rem/68, byline roles 33rem/67, process keytext 24rem/67, process rule 33rem/59, figures note 24rem, foot signoff and foot where 18rem/48 (footer columns, short by design), foot legal 23rem/67, contact line 19rem/47, founders standing 38rem/50. Each was re-checked at 820px as well as 1280px because both faces are fluid. The five caps still in ch are all line-break controls on fluid display headlines (.v1-work__title, .v1-engagement__title, .v1-process__title, .v1-faq__title, .v1-contact__title), where ch is the unit that tracks the headline's own size and rem cannot; the one rule and that one exception are now written into tokens.css over .v1-prose as the MEASURE note.
- [done] not on this list, from the task: tokens.css section 7 now records that `.v1-draw__ink`'s dasharray needs `pathLength="100"` (the dash length is the literal 100, so without it a long path draws dotted and a short one solid), and that a non-uniformly scaled SVG breaks it unless the drawn paths take `vector-effect: non-scaling-stroke`.
